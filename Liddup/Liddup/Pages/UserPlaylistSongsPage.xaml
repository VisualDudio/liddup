<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Liddup.UserPlaylistSongsPage"
             x:Name="UserPlaylistSongsContentPage">
    <ContentPage.Content>
        <StackLayout>
            <ActivityIndicator IsRunning="{Binding Source={x:Reference UserPlaylistSongsContentPage}, Path=IsBusy}" Color="Blue"/>
            <ListView x:Name="UserPlaylistSongs" ItemSelected="UserPlaylistSongs_OnItemSelected" CachingStrategy="RecycleElementAndDataTemplate">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>